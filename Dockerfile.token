# Dockerfile.token — Token server
FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir fastapi uvicorn livekit python-dotenv

COPY agent/token_server.py .

CMD ["uvicorn", "token_server:app", "--host", "0.0.0.0", "--port", "8080"]
